import RegisterForm from '../../components/auth/RegisterForm';

export const metadata = {
  title: 'Create Account - TravelCircles',
  description: 'Join TravelCircles to book bus tickets and connect with a community of travelers.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
