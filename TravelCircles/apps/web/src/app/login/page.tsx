import LoginForm from '../../components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - TravelCircles',
  description: 'Sign in to your TravelCircles account to book tickets and connect with fellow travelers across India.',
};

export default function LoginPage() {
  return <LoginForm />;
}
